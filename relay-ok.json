{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/nostr-protocol/nips/schemas/relay-ok.json",
  "title": "Nostr Relay OK Message",
  "description": "Relay to client OK message as defined in NIP-01",
  "type": "array",
  "minItems": 4,
  "maxItems": 4,
  "items": [
    {
      "type": "string",
      "const": "OK"
    },
    {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Event ID"
    },
    {
      "type": "boolean",
      "description": "True if accepted, false if rejected"
    },
    {
      "type": "string",
      "description": "Message (empty string if accepted, machine-readable prefix + human-readable message if rejected)"
    }
  ]
}
